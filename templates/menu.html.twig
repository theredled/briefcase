
<div class="section-h2">
    {% for token, label  in menu -%}
        {%- if token == selected_token -%}
            <h2 class="selected menu-item">
        {%- else -%}
            <a href="{{ path('home') }}#{{ token }}" class="menu-item">
        {%- endif -%}
        {{- label -}}
        {%- if token == selected_token -%}
            </h2>
        {%- else -%}
            </a>
        {%- endif -%}
    {%- endfor -%}
    {# <a href="{{ path('dl_index') }}" class="menu-item">téléchargements</a>  #}
</div>