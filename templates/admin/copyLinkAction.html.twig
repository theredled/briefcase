{% set url = action.getLinkUrl() %}
<p class="copy-url-success-message hidden">Lien copié !</p>
<a href="{{ url }}" class="copy-url-btn" data-url="{{ absolute_url(url) }}">
    <i class="fa fa-copy"></i>
</a>